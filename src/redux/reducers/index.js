const initialState = {
  favourites: {
    content: []
  }
};

const MainReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default MainReducer;
